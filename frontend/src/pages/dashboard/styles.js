import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  widgetText: {
    fontSize: '1.5rem',
    fontWeight: 'bold',
  },
  widgetTextCount: {
    fontSize: '2.5rem',
  },
}));
